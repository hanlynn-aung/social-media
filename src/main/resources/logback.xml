<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Log directory and file -->
    <property name="LOG_DIR" value="logs"/>
    <property name="LOG_FILE" value="${LOG_DIR}/application.log"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p ${PID:- } --- [%15.15t] %-40.40logger{39} : %m%n"/>
    
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- File Appender (Rolling) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/application.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>
    
    <!-- Async Appender for Performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="FILE"/>
    </appender>
    
    <!-- Application Logs -->
    <logger name="com.example.socialmedia" level="DEBUG"/>
    
    <!-- Security Logs (High Priority) -->
    <logger name="com.example.socialmedia.security" level="INFO"/>
    <logger name="com.example.socialmedia.audit" level="INFO"/>
    <logger name="com.example.socialmedia.config" level="INFO"/>
    
    <!-- Spring Framework -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.security" level="DEBUG"/>
    <logger name="org.springframework.web" level="INFO"/>
    
    <!-- Database -->
    <logger name="org.hibernate" level="INFO"/>
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
    <logger name="org.springframework.data" level="DEBUG"/>
    
    <!-- MongoDB -->
    <logger name="org.mongodb" level="INFO"/>
    <logger name="org.springframework.data.mongodb" level="DEBUG"/>
    
    <!-- Bucket4j (Rate Limiting) -->
    <logger name="io.github.bucket4j" level="INFO"/>
    
    <!-- Third-party Libraries -->
    <logger name="com.zaxxer.hikari" level="INFO"/>
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>
    
    <!-- Development Profile -->
    <springProfile name="dev,default">
        <root level="DEBUG">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- Production Profile -->
    <springProfile name="prod">
        <root level="WARN">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- Docker Profile -->
    <springProfile name="docker">
        <root level="INFO">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- Advanced Security Profile -->
    <springProfile name="advanced-security">
        <logger name="com.example.socialmedia.security" level="DEBUG"/>
        <logger name="com.example.socialmedia.audit" level="DEBUG"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
    </springProfile>
    
    <!-- Default Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>
</configuration>
